{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-secondary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }} #}

		<div class="form-group col col-lg-6 col-md-12">
				{{ form_start(form) }}
				<div class="form-group mb-3">
					{{ form_row(form.rate, {'attr': {'class': 'form-range'}}) }}
				</div>
				<div class="form-group mb-3">
					{{ form_row(form.comment, {'attr': {'class': 'form-control'}}) }}
				</div>
				<div class="form-group mb-3">
					<input type="submit" value="Envoyer" class="btn btn-secondary">
				</div>
				{{ form_end(form) }}
		</div>