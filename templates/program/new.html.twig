{% extends 'base.html.twig' %}

{% block title %}Add a program{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type='submit' value='Add program'>
    {{ form_end(form) }}

    <a class="mt-3 btn btn-primary" href="{{ path('program_index') }}">
        Retour
    </a>
    <a class="mt-3 btn btn-primary" href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}
