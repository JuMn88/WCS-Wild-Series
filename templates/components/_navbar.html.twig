<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <img class="mx-2" src="{{ asset('build/logo.png') }}" width="30" height="30" alt="{{ 'img.logo_alt'|trans }}">
  <!-- <div>Icônes conçues par <a href="https://www.freepik.com" title="Freepik">Freepik</a> from <a href="https://www.flaticon.com/fr/" title="Flaticon">www.flaticon.com</a></div> -->
  <a class="navbar-brand mx-2" href="{{ path('app_index') }}">{{ 'nav.index'|trans }}</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="nav-item dropdown">
      <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        {{ 'nav.categories'|trans }}
      </a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <a class="dropdown-item" href="{{ path('category_index') }}">{{ 'categories.index_title'|trans }}</a>
        <li><hr class="dropdown-divider"></li>
        {{ render(controller('App\\Controller\\DefaultController::navbarTop')) }}
      </ul>
    </div>
    <a class="nav-link text-light" href="{{ path('program_index') }}">{{ 'nav.programs'|trans }}</a>
    <a class="nav-link text-light" href="{{ path('episode_index') }}">{{ 'nav.episodes'|trans }}</a>
    <a class="nav-link text-light" href="{{ path('actor_index') }}">{{ 'nav.casting'|trans }}</a>
    <div class="nav-item dropdown">
      <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="bi bi-globe"></i> {{ app.request.locale|language_name|capitalize }}
      </a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <li><a class="dropdown-item" href="{{ path('app_change_locale', { locale: 'fr' }) }}">🇫🇷 - {{ 'fr'|language_name|capitalize }}</a></li>
        <li><a class="dropdown-item" href="{{ path('app_change_locale', { locale: 'en' }) }}">🇬🇧 - {{ 'en'|language_name|capitalize }}</a></li>
      </ul>
    </div>
    {% if app.user %}
      <a class="nav-link text-light" href="{{ path('app_profile') }}">
        {% trans with {'%username%': app.user.username} %}
          hello_username
        {% endtrans %} !
      </a>
      <a class="nav-link text-light" href="{{ path('app_logout') }}">{{ 'account.logout'|trans }}</a>
    {% else %}
      <a class="nav-link text-light" href="{{ path('app_login') }}">{{ 'account.login'|trans }}</a>
    {% endif %}
  </div>
</nav>